# 4. Median of Two Sorted Arrays

## Approach
- Store both arrays in a new array.
- Sort the combined array.
- Find the middle element(s) and calculate the median.

## Complexity
- **Time:** O((n + m) log(n + m))
- **Space:** O(n + m)